# フェーズ0事前検証記録

## 1. 目的

この文書は、[`07_実装工程詳細.md`](../企画書/07_実装工程詳細.md)のフェーズ0で必要な実機検証結果を記録する。未実測の項目は推測で完了扱いにしない。

検証日: 2026-09-18

## 2. 現在の実測結果

| 項目 | 状態 | 実測結果 |
| --- | --- | --- |
| 開発用Python | 確認済み | `G:\LoraMaker\.venv`をPython 3.14.7で作成。`.python-version`へ固定版を記録 |
| Node.js / npm | 確認済み | fnm 1.39.0でNode.js 26.9.0を管理し、PowerShell新規セッションでnpm 11.19.1とともにPATH経由で確認 |
| FastAPI / Pydantic | 確認済み | プロジェクトvenvへFastAPI 0.141.1、Pydantic 2.13.5を導入し、importとバージョンを確認 |
| React / Vite | 確認済み | 隔離検証フォルダへReact 19.3.0とVite 8.3.0を完全一致指定で導入し、npm監査0件を確認 |
| Stability Matrix | 確認済み | `G:\StabilityMatrix\StabilityMatrix.exe` |
| ComfyUIパッケージ | 確認済み | `G:\StabilityMatrix\Data\Packages\ComfyUI`。付属Pythonは3.12.11 |
| ComfyUI API | 確認済み | `http://127.0.0.1:8188/system_stats`へ接続成功。ComfyUI 0.36.0、付属Python 3.12.11、PyTorch 2.14.0+cu130を確認 |
| kohya_ssパッケージ | 確認済み | `G:\StabilityMatrix\Data\Packages\kohya_ss` |
| sd-scripts CLI読込 | 確認済み | kohya_ss付属Python 3.10.18で`train_network.py --help`を表示できた |
| GPU認識 | 確認済み | PyTorch 2.7.0+cu128、CUDA利用可、NVIDIA GeForce RTX 4090 |
| sd-scripts最小学習 | 確認済み | kohya_ss付属Pythonから`sdxl_train_network.py`を直接起動し、1ステップ学習とsafetensors保存に成功 |
| BandiView | 確認済み | 7.28.0.1を抽出済みフレームフォルダ指定で起動し、サブ写真フォルダへ選別画像をコピー。元画像との同一性を確認 |
| ffmpeg | 確認済み | FFmpeg / ffprobe 9.0.1をPATH経由で起動し、H.264 MP4の全161フレームをPNGへ抽出 |
| 動画生成ワークフロー | 確認済み | API形式の`comfyui_workflows/movie_turn.json`を実行し、MP4を取得 |
| 動画生成専用一時出力 | 確認済み | 検証用ComfyUIの`--output-directory`へ`workspace/comfyui/video-temp`を指定し、保存・移動・空フォルダ化に成功 |
| 画像拡大ワークフロー | 確認済み | API形式の`comfyui_workflows/image_upscale.json`を入力画像と出力接頭辞だけメモリ上で差し替えて実行し、4096×4096 PNGを取得 |
| Taggerワークフロー | 確認済み | API形式の`comfyui_workflows/tagger.json`を対象1枚に制限して実行し、TXTを取得 |
| ComfyUI / sd-scripts GPU排他 | 確認済み | ComfyUIモデル解放後、APIサーバーを停止せず学習に成功。学習ピーク8,394MiB、動画生成ピーク20,691MiBのためGPU処理は排他とする |

`train_network.py --help`実行時にWindows環境では任意依存のTritonが存在しない旨の警告が出た。ヘルプ表示自体は成功しており、最小学習の成否は別途確認する。

### 2.1 ComfyUI画像拡大の実行記録

| 項目 | 実測値 |
| --- | --- |
| API URL | `http://127.0.0.1:8188` |
| ワークフロー | `G:\LoraMaker\comfyui_workflows\image_upscale.json` |
| 画像入力ノード | `1` / `LoadImage` / `image` |
| 入力画像 | `sample01_00061_.png` |
| モデルノード | `2` / `UpscaleModelLoader` / `RealESRGAN_x4plus_anime_6B.pth` |
| 回収対象出力ノード | `4` / `SaveImage` |
| 完了判定 | `/history/{prompt_id}`の`status.completed=true`かつ`status.status_str=success` |
| prompt ID | `0c7eff9b-bde7-4459-ac8b-468999204c34` |
| 出力 | `G:\StabilityMatrix\Data\Packages\ComfyUI\output\LoRAMaker_phase0\image_upscale_repo_00001_.png` |
| 出力形式 | PNG、4096×4096、2,820,880 bytes |

保存済みの`image_upscale.json`原本は変更せず、要求時に入力画像と`filename_prefix`をメモリ上で差し替えた。ComfyUI履歴から成功状態と出力ファイルを取得し、実ファイルの存在とサイズを確認した。

### 2.2 ComfyUI Taggerの実行記録

| 項目 | 実測値 |
| --- | --- |
| ワークフロー | `G:\LoraMaker\comfyui_workflows\tagger.json` |
| 画像入力ノード | `1` / `VHS_LoadImagesPath` / `directory` |
| 件数制御 | `image_load_cap=1` |
| Taggerノード | `5` / `WD14Tagger\|pysssss` / `wd-eva02-large-tagger-v3` |
| 回収対象出力ノード | `18` / `SaveText` |
| 完了判定 | `/history/{prompt_id}`の`status.completed=true`かつ`status.status_str=success` |
| prompt ID | `a5792c4d-44d2-41d2-890b-f67e71fa5d5a` |
| 出力 | `G:\StabilityMatrix\Data\Packages\ComfyUI\output\LoRAMaker_phase0\tagger_00001.txt` |
| 出力形式 | TXT、361 bytes |

### 2.3 ComfyUI動画生成の実行記録

| 項目 | 実測値 |
| --- | --- |
| ワークフロー | `G:\LoraMaker\comfyui_workflows\movie_turn.json` |
| 画像入力ノード | `97` / `LoadImage` / `image` |
| プロンプト入力ノード | `124` / `promptComposerTextMultiple` |
| ネガティブ入力ノード | `125` / `promptComposerTextMultiple` |
| 動画構築ノード | `94` / `CreateVideo` / 16fps |
| 回収対象出力ノード | `108` / `SaveVideo` |
| 完了判定 | `/history/{prompt_id}`の`status.completed=true`かつ`status.status_str=success` |
| prompt ID | `952987d2-197e-465d-88ed-08db3d6af612` |
| 実行時間 | 約75.5秒 |
| 実行中VRAM | 最大実測20,588MiB使用 / 24,564MiB |
| 出力 | `G:\StabilityMatrix\Data\Packages\ComfyUI\output\LoRAMaker_phase0\movie_turn_00001_.mp4` |
| 出力形式 | MP4、1,042,241 bytes |

元ワークフローは変更せず、要求時に`filename_prefix`だけをメモリ上で検証用へ差し替えた。入力画像、モデル、LoRAおよびプロンプトは保存済みワークフローの設定を使用した。

### 2.4 sd-scripts最小学習の実行記録

| 項目 | 実測値 |
| --- | --- |
| Python | `G:\StabilityMatrix\Data\Packages\kohya_ss\venv\Scripts\python.exe` / Python 3.10.18 |
| 学習スクリプト | `G:\StabilityMatrix\Data\Packages\kohya_ss\sd-scripts\sdxl_train_network.py` |
| 作業ディレクトリ | `G:\StabilityMatrix\Data\Packages\kohya_ss\sd-scripts` |
| 検証用設定 | `G:\LoraMaker\phase0\sd-scripts-minimal.toml` |
| 学習素材 | `C:\Users\nin65\Desktop\stabledifusion\lora\260916_white\src01\10_body` |
| 学習データ | 画像1枚、同名キャプション1個、10反復 |
| ベースモデル | `G:\StabilityMatrix\Data\Models\StableDiffusion\boleromixIllustrious_v900.safetensors` |
| 実行条件 | SDXL、1024×1024、batch size 1、1 epoch、最大1 step、AdamW、fp8 base |
| 完了判定 | プロセス終了コード0、`model saved`ログ、および成果物の存在確認 |
| 成果物 | `G:\LoraMaker\phase0-output\sd-scripts\phase0_sd_scripts_test.safetensors` |
| 成果物サイズ | 42,864,356 bytes |
| SHA-256 | `B07D043115D1544FB4D72D3680984D54C4B0627C0835BD8AF8E7B0C553BC6FB7` |
| 学習結果 | 1/1 step完了、平均loss 0.06 |

通常SD用の`train_network.py`ではSDXLチェックポイントとのUNet形状不一致になった。既存のkohya_ss設定JSONに`sdxl: true`が記録されていることを確認し、`sdxl_train_network.py`へ切り替えることで成功した。Windowsでは任意依存のTritonがない旨の警告が出るが、xformersを用いた1ステップ学習と成果物保存は正常に完了した。

学習前にComfyUIの`/free`へ`unload_models=true`と`free_memory=true`を送信し、VRAM使用量を約2.6GBまで下げた。ComfyUIサーバーを停止せずに学習を完了し、学習後もAPI接続を確認できた。この結果から、少なくとも今回の構成ではComfyUIプロセス停止は必須ではなく、学習前のモデル解放とGPUジョブ排他で運用可能である。

### 2.5 FFmpeg全フレーム抽出の実行記録

| 項目 | 実測値 |
| --- | --- |
| FFmpeg / ffprobe | 9.0.1 full build / Gyan.dev |
| 入力 | `G:\StabilityMatrix\Data\Packages\ComfyUI\output\LoRAMaker_phase0\movie_turn_00001_.mp4` |
| 入力形式 | H.264、512×512、16fps、約10.06秒 |
| ffprobe読取フレーム数 | 161 |
| 抽出コマンドの要点 | `ffmpeg -i <input> -fps_mode passthrough <output>\frame_%06d.png` |
| 出力 | `G:\LoraMaker\phase0-output\ffmpeg\movie_turn_00001` |
| 抽出結果 | PNG 161枚、`frame_000001.png`〜`frame_000161.png`、合計22,966,511 bytes |

FFmpeg 9.0.1では従来の`-vsync 0`が認識されなかったため、現行オプションの`-fps_mode passthrough`を使用した。ffprobeの読取数と抽出ファイル数が一致しており、入力動画を変更せず全フレームを取得できた。

### 2.6 Node.js・npm・フロントエンド依存の実行記録

| 項目 | 実測値 |
| --- | --- |
| バージョン管理 | fnm 1.39.0 |
| PowerShell初期化 | `$PROFILE`で`fnm env --use-on-cd --shell powershell`を実行 |
| プロジェクト固定 | `G:\LoraMaker\.node-version` / 26.9.0 |
| Node.js | v26.9.0 |
| npm | 11.19.1 |
| 検証ディレクトリ | `G:\LoraMaker\phase0-output\node-smoke` |
| React | 19.3.0 / 完全一致指定 |
| Vite | 8.3.0 / 完全一致指定 |
| npm監査 | 17パッケージ、脆弱性0件 |

新しいPowerShellセッションでプロジェクトへ移動すると、`.node-version`によりNode.js 26.9.0が選択された。ReactとViteは本番構成を作る前の隔離検証として導入し、固定対象バージョンをnpmから取得できることを確認した。

### 2.7 BandiViewフォルダ指定起動の実行記録

| 項目 | 実測値 |
| --- | --- |
| BandiView | `C:\Program Files\BandiView\BandiView.exe` / 7.28.0.1 |
| 対象フォルダ | `G:\LoraMaker\phase0-output\ffmpeg\movie_turn_00001` |
| 対象画像 | PNG 161枚 |
| 起動方法 | `BandiView.exe "<対象フォルダ>"` |
| 結果 | プロセス起動成功。OSのプロセス情報から対象フォルダがコマンドライン引数として渡されたことを確認 |

サブ写真フォルダを`G:\LoraMaker\workspace\images\selected`へ設定し、`frame_000068.png`をコピーした。コピー先は1ファイル、元フォルダは161ファイルのままであり、コピー画像と元画像のSHA-256が`CB36D24E82F63C60F113656FDC757BA82A5FEBA5888B0ECC792EE00061AE0ACC`で一致した。これにより、元画像を保持したまま選別画像を固定保存先へコピーできることを確認した。

### 2.8 ComfyUI動画専用一時出力の実行記録

| 項目 | 実測値 |
| --- | --- |
| 一時出力の実体 | `G:\LoraMaker\workspace\comfyui\video-temp` |
| ComfyUI起動引数 | `--output-directory G:\LoraMaker\workspace\comfyui\video-temp` |
| 検証用API | `http://127.0.0.1:8189` |
| 最小入力 | 画像1枚から1fps・1フレーム動画を作成 |
| 一時出力 | `phase0_probe\minimal_video_00001_.mp4` / 79,709 bytes |
| 保全先 | `G:\LoraMaker\phase0-output\comfyui-temp-test-result\minimal_video_00001_.mp4` |
| 後処理 | 保全先への移動確認後、一時出力内の残存ファイル0件を確認 |

通常起動中のComfyUIとは別に、出力先だけを変更した検証用ComfyUIをポート8189で起動して確認した。検証後は同プロセスだけを終了し、通常のポート8188へのAPI接続が継続していることを確認した。

ComfyUIの保存処理は実パスを検証するため、標準output配下からworkspaceへのディレクトリジャンクションは「output外への保存」として拒否された。この安全機構は迂回せず、workspaceを使用する場合はComfyUI起動時の`--output-directory`で正式に指定する。

### 2.9 sd-scripts学習中のVRAM計測記録

| 項目 | 実測値 |
| --- | --- |
| 計測間隔 | 約250ms |
| 計測サンプル数 | 86 |
| GPU総容量 | 24,564MiB |
| 学習前使用量 | 2,722MiB |
| 学習中ピーク | 8,394MiB |
| 学習による最大増分 | 5,672MiB |
| ピーク時残容量の目安 | 16,170MiB |
| プロセス実行時間 | 約25.3秒 |
| 終了コード | 0 |
| 学習 | 1/1 step、平均loss 0.0238 |
| 成果物 | `G:\LoraMaker\phase0-output\sd-scripts\phase0_sd_scripts_vram_test.safetensors` / 42,864,340 bytes |
| SHA-256 | `598935101A07084267E0260747F8E9DFB82AE5420737CEA0CAB0A9AA670E478B` |

ComfyUIのAPIサーバーは停止せず、学習前に`/free`でモデルを解放した状態で計測した。動画生成時の最大実測20,691MiBと学習時の最大実測8,394MiBを合算するとGPU総容量を超えるため、ComfyUI生成ジョブとsd-scripts学習ジョブは同時実行しない。学習時にComfyUIプロセス自体を停止する必要はなく、モデル解放とジョブ排他を必須とする。

## 3. 再検証方法

```powershell
.\scripts\phase0-preflight.ps1
```

配置が異なる場合は`StabilityMatrixRoot`、`ComfyUiApiUrl`、`BandiViewPath`を引数で指定する。スクリプトは読み取りとAPI疎通だけを行い、外部アプリの起動、ファイル削除、ワークフロー投入および学習開始は行わない。必須項目に失敗がある場合は終了コード1を返す。

## 4. 後続フェーズへの影響

| 未解決事項 | 停止する後続フェーズ |
| --- | --- |
| なし | 後続フェーズを停止する未解決事項なし |

## 5. フェーズ0完了判定

外部ツールごとの最小入出力、固定開発環境、API・コマンド・パス、sd-scripts直接実行、GPU排他方針および後続フェーズへの影響を実測・記録した。フェーズ0の完了条件を満たしたため、[`06_実装ロードマップ.md`](../企画書/06_実装ロードマップ.md)へ完了チェックを付ける。
